# Claude Instructions

- Commit after each change, push periodically
- Do NOT deploy unless requested
- SvelteKit 5 + TypeScript, Firebase Hosting
- `npm run dev` / `npm run build` / `firebase deploy`
